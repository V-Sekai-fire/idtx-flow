exclude: '(^|/)proto_gen/|^shared/include/idtx/'

repos:
  - repo: https://github.com/pre-commit/mirrors-clang-format
    rev: v22.1.5
    hooks:
      - id: clang-format
        files: ^(source|shared)/.*\.(h|cpp)$
